syntax = "proto2";
package main;

message BhMessage {
	enum BhMessageType {
		BH_PING = 1;
		BH_PEERS = 2;
	};

	required BhMessageType type = 1;
	message Peer {
		required bytes id = 1;
		repeated bytes addrs = 2;
	};
	repeated Peer peers = 2;
};

